---
import BaseLayout from '../layouts/BaseLayout.astro';

const pageTitle = "Digital Credibility Score 900+ Websites | NexTara AI Solutions";
const pageDescription = "Is your website producing the visibility, trust, and leads you expected? We rebuild websites to align with how search and AI discovery actually works now.";
---

<BaseLayout title={pageTitle} description={pageDescription}>
  <main>
    <!-- SECTION 1: HERO BLOCK -->
    <section class="hero-section">
      <div class="container">
        <div class="hero-grid">
          <!-- Left Column: Existing Hero Text -->
          <div class="hero-content">
            <h1 class="hero-title fade-up">
              Digital Credibility Score 900+ Websites
            </h1>
            <p class="hero-lead fade-up-delay-1">
              Is your website producing the visibility, trust, and leads you expected?
            </p>
            <p class="hero-support fade-up-delay-2">
              Most websites look good on the surface but fail modern visibility tests.
              Today, Google and AI systems evaluate your technical integrity, schema structure, accessibility, and analytics correctness long before they reward your design.
              We rebuild websites to align with the way search actually works now.
            </p>
            <div class="hero-ctas fade-up-delay-3">
              <a href="/digital-credibility-score-audit" class="btn btn-primary">
                Get My Free Digital Credibility Score Audit
              </a>
              <a href="/how-dcs-works" class="btn btn-secondary">
                How DCS 900+ Websites Work
              </a>
            </div>
          </div>

          <!-- Right Column: DCS Audit Diagnostic Card (decorative) -->
          <div class="hero-visual-card card-elevate" aria-hidden="true">
            <div class="visual-header">DCS Audit Overview</div>

            <div class="visual-metric">
              <span class="metric-label">Technical Integrity</span>
              <div class="metric-bar"><div class="metric-bar-fill metric-fill-1"></div></div>
            </div>

            <div class="visual-metric">
              <span class="metric-label">Accessibility</span>
              <div class="metric-bar"><div class="metric-bar-fill metric-fill-2"></div></div>
            </div>

            <div class="visual-metric">
              <span class="metric-label">Analytics Integrity</span>
              <div class="metric-bar"><div class="metric-bar-fill metric-fill-3"></div></div>
            </div>

            <div class="visual-footer">Example Visualization</div>
          </div>
        </div>
      </div>
    </section>

    <!-- SECTION 2: AI-SEARCH / MODERN VISIBILITY BLOCK -->
    <section class="visibility-section" aria-labelledby="visibility-heading">
      <div class="container">
        <div class="two-column-layout">
          <div class="content-column">
            <h2 id="visibility-heading">
              Search Didn't Stand Still. Most Websites Did.
            </h2>
            <p class="section-paragraph">
              Search and AI discovery systems now rely on structured data, accessibility correctness, and analytics integrity to determine trust.
              Most websites built before 2024 never accounted for these requirements — which is why they underperform even when they look polished.
            </p>
            <ul class="feature-list">
              <li>Technical integrity</li>
              <li>Schema and entity structure</li>
              <li>WCAG 2.2 AA accessibility</li>
              <li>GTM/GA4 analytics correctness</li>
            </ul>
          </div>
          <div class="visual-column">
            <div class="visual-placeholder"></div>
          </div>
        </div>
      </div>
    </section>

    <!-- SECTION 3: DUAL-PERSONA VALUE BLOCK -->
    <section class="value-section" aria-labelledby="value-heading">
      <div class="container">
        <h2 id="value-heading" class="section-title">
          Two Ways NexTara Helps You Win
        </h2>
        <div class="dual-card-grid">
          <div class="value-card">
            <h3 class="card-headline">Fully Managed Website Rebuild</h3>
            <ul class="card-bullets">
              <li>DCS 900+ website implementation</li>
              <li>SEO, accessibility, and performance baked into the core</li>
              <li>Analytics-first build for long-term visibility</li>
            </ul>
            <a href="/rebuild-process" class="micro-cta">See the Rebuild Process →</a>
          </div>
          
          <div class="value-card">
            <h3 class="card-headline">Enterprise Governance & DIOS</h3>
            <ul class="card-bullets">
              <li>DIOS-compliant tracking and audit readiness</li>
              <li>Tier-1 analytics integrity</li>
              <li>Scalable multi-site standards</li>
            </ul>
            <a href="/governance-suite" class="micro-cta">View Governance Suite →</a>
          </div>
        </div>
      </div>
    </section>

    <!-- SECTION 4: SERVICES SNAPSHOT BLOCK -->
    <section class="services-section" aria-labelledby="services-heading">
      <div class="container">
        <h2 id="services-heading" class="section-title">
          What We Deliver
        </h2>
        <div class="services-grid">
          <div class="service-card">
            <h3 class="service-title">DCS Website Rebuilds</h3>
            <p class="service-description">
              Ground-up rebuilds engineered for modern search, AI discovery, and analytics integrity. Built to DCS 900+ standards from day one.
            </p>
          </div>

          <div class="service-card">
            <h3 class="service-title">Analytics Integrity & DIOS</h3>
            <p class="service-description">
              DIOS-compliant tracking architecture, governance-ready analytics, and audit-proof data pipelines for enterprise organizations.
            </p>
          </div>

          <div class="service-card">
            <h3 class="service-title">Google Ads Compliance Management</h3>
            <p class="service-description">
              Policy-compliant campaigns, tracking verification, and ongoing compliance monitoring to prevent disapprovals and account suspensions.
            </p>
          </div>

          <div class="service-card">
            <h3 class="service-title">Ecosystem Command Center (Pilot)</h3>
            <p class="service-description">
              Unified dashboard for managing analytics, ads compliance, SEO health, and technical performance across your entire digital ecosystem.
            </p>
          </div>
        </div>
      </div>
    </section>

    <!-- SECTION 5: TRUST LAYER BLOCK -->
    <section class="trust-section" aria-label="Trust indicators">
      <div class="container">
        <div class="trust-content">
          <div class="trust-badge">
            <span class="badge-text">DCS 900+</span>
          </div>
          <div class="trust-message">
            <p>
              Built for WCAG 2.2 AA, analytics integrity, and performance — not just surface-level design.
            </p>
          </div>
        </div>
      </div>
    </section>

    <!-- SECTION 6: CLOSING CTA BLOCK -->
    <section class="closing-cta-section" aria-labelledby="closing-cta-heading">
      <div class="container">
        <h2 id="closing-cta-heading" class="closing-title">
          Start With a Frictionless Digital Credibility Score Audit
        </h2>
        <p class="closing-paragraph">
          You'll see exactly where your website stands, what's holding it back, and what your upgrade path looks like — no jargon, no pressure, and no obligation.
        </p>
        <div class="cta-group-centered">
          <a href="/digital-credibility-score-audit" class="cta-button cta-primary-large">
            Get My Audit Report
          </a>
        </div>
      </div>
    </section>
  </main>
</BaseLayout>

<style>
  /* ==========================================
     GLOBAL TYPOGRAPHY & SPACING
     ========================================== */
  
  :root {
    /* Typography Scale */
    --font-size-h1: clamp(48px, 5vw, 64px);
    --font-size-h2: clamp(32px, 4vw, 40px);
    --font-size-h3: clamp(24px, 2.5vw, 28px);
    --font-size-body: clamp(18px, 1.5vw, 20px);
    --line-height-base: 1.6;
    
    /* Spacing Tokens */
    --section-padding-y: clamp(80px, 10vw, 140px);
    --card-padding: clamp(32px, 4vw, 40px);
    --grid-gap: clamp(24px, 3vw, 32px);
    --paragraph-spacing: clamp(18px, 2vw, 24px);
  }

  main {
    width: 100%;
  }

  .container {
  max-width: 1340px;
  margin: 0 auto;
  padding-left: 24px;
  padding-right: 24px;
}

/* Hero section spacing */
.hero-section {
  background: var(--color-bg, #0a0f18);
  padding-top: clamp(64px, 8vw, 88px);
  padding-bottom: clamp(96px, 12vw, 128px);
}
  /* ==========================================
     SECTION 1: HERO BLOCK
     ========================================== */
  
  .hero-section {
    padding: var(--section-padding-y) 0;
    min-height: 70vh;
    display: flex;
    align-items: center;
  }

  .hero-title {
  font-size: var(--font-size-h1);
  font-weight: 700;
  line-height: 1.1;
  letter-spacing: -0.015em;
  margin-bottom: 32px;
  color: var(--color-text-primary, #f8fafc);
  animation: fadeUp 0.6s ease-out;
}

  .hero-lead {
    font-size: var(--font-size-h2);
    font-weight: 600;
    line-height: 1.3;
    margin-bottom: 24px;
    color: var(--color-primary, #60a5fa);
    animation: fadeUp 0.7s ease-out;
  }

  .hero-support {
  font-size: clamp(19px, 1.9vw, 21px);
  line-height: 1.6;
  margin-bottom: 48px;
  max-width: 720px;
  color: var(--color-text-soft, #cbd5e1);
  animation: fadeUp 0.8s ease-out;
}

  .cta-group {
    display: flex;
    gap: 16px;
    flex-wrap: wrap;
    animation: fadeUp 0.9s ease-out;
  }

  /* ==========================================
     SECTION 2: VISIBILITY BLOCK
     ========================================== */
  
  .visibility-section {
    padding: var(--section-padding-y) 0;
    background: var(--color-surface, #0f1624);
  }

  .two-column-layout {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: var(--grid-gap);
    align-items: center;
  }

  .section-paragraph {
    font-size: var(--font-size-body);
    line-height: var(--line-height-base);
    margin-bottom: var(--paragraph-spacing);
    color: var(--color-text-soft, #cbd5e1);
  }

  .feature-list {
    list-style: none;
    padding: 0;
    margin: 32px 0 0 0;
  }

  .feature-list li {
    font-size: var(--font-size-body);
    padding: 10px 0 10px 32px;
    position: relative;
    color: var(--color-text, #f8fafc);
  }

  .feature-list li::before {
    position: absolute;
    left: 0;
    color: var(--color-primary, #60a5fa);
    font-weight: bold;
    font-size: 20px;
  }

  .visual-placeholder {
    width: 100%;
    height: 300px;
    background: var(--color-surface-raised, #141d2e);
    border-radius: 8px;
    border: 1px solid var(--color-border, #1e293b);
  }

  /* ==========================================
     SECTION 3: DUAL-PERSONA VALUE BLOCK
     ========================================== */
  
  .value-section {
    padding: var(--section-padding-y) 0;
  }

  .section-title {
    font-size: var(--font-size-h2);
    text-align: center;
    margin-bottom: 64px;
    color: var(--color-text-primary, #f8fafc);
  }

  .dual-card-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: var(--grid-gap);
  }

  .value-card {
    background: var(--color-surface, #0f1624);
    border: 1px solid var(--color-border, #1e293b);
    border-radius: 12px;
    padding: var(--card-padding);
    transition: all 0.2s ease;
  }

  .value-card:hover {
    transform: translateY(-4px);
    box-shadow: 0 8px 24px rgba(59, 130, 246, 0.15);
  }

  .card-headline {
    font-size: var(--font-size-h3);
    margin-bottom: 24px;
    color: var(--color-text-primary, #f8fafc);
  }

  .card-bullets {
    list-style: none;
    padding: 0;
    margin: 0 0 24px 0;
  }

  .card-bullets li {
    padding: 8px 0 8px 24px;
    position: relative;
    font-size: 18px;
    color: var(--color-text-soft, #cbd5e1);
  }

  .card-bullets li::before {
    content: "→";
    position: absolute;
    left: 0;
    color: var(--color-primary, #60a5fa);
    font-weight: bold;
  }

  .micro-cta {
    display: inline-block;
    font-size: 16px;
    font-weight: 600;
    color: var(--color-primary, #60a5fa);
    text-decoration: none;
    transition: all 0.2s ease;
  }

  .micro-cta:hover {
    color: var(--color-primary-hover, #2563eb);
    transform: translateX(4px);
  }

  /* ==========================================
     SECTION 4: SERVICES SNAPSHOT
     ========================================== */
  
  .services-section {
    padding: var(--section-padding-y) 0;
    background: var(--color-surface, #0f1624);
  }

  .services-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: var(--grid-gap);
  }

  .service-card {
    background: var(--color-bg, #0a0f18);
    border: 1px solid var(--color-border, #1e293b);
    border-radius: 12px;
    padding: var(--card-padding);
    transition: all 0.2s ease;
  }

  .service-card:hover {
    transform: translateY(-4px);
    box-shadow: 0 8px 24px rgba(0, 0, 0, 0.3);
  }

  .service-icon {
    font-size: 40px;
    margin-bottom: 16px;
  }

  .service-title {
    font-size: var(--font-size-h3);
    margin-bottom: 12px;
    color: var(--color-text-primary, #f8fafc);
  }

  .service-description {
    font-size: 16px;
    line-height: 1.6;
    color: var(--color-text-muted, #94a3b8);
  }

  /* ==========================================
     SECTION 5: TRUST LAYER
     ========================================== */
  
  .trust-section {
    padding: 60px 0;
    background: var(--color-surface-raised, #141d2e);
    border-top: 1px solid var(--color-border, #1e293b);
    border-bottom: 1px solid var(--color-border, #1e293b);
  }

  .trust-content {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 48px;
  }

  .trust-badge {
    background: var(--color-primary-soft, rgba(59, 130, 246, 0.1));
    border: 2px solid var(--color-primary, #60a5fa);
    border-radius: 12px;
    padding: 24px 40px;
  }

  .badge-text {
    font-size: 32px;
    font-weight: 700;
    color: var(--color-primary, #60a5fa);
  }

  .trust-message p {
    font-size: 18px;
    line-height: 1.6;
    max-width: 500px;
    color: var(--color-text-soft, #cbd5e1);
    margin: 0;
  }

  /* ==========================================
     SECTION 6: CLOSING CTA
     ========================================== */
  
  .closing-cta-section {
    padding: var(--section-padding-y) 0;
    text-align: center;
  }

  .closing-title {
    font-size: var(--font-size-h2);
    margin-bottom: 24px;
    color: var(--color-text-primary, #f8fafc);
  }

  .closing-paragraph {
    font-size: var(--font-size-body);
    line-height: var(--line-height-base);
    max-width: 700px;
    margin: 0 auto 48px;
    color: var(--color-text-soft, #cbd5e1);
  }

  .cta-group-centered {
    display: flex;
    justify-content: center;
  }

  /* ==========================================
     CTA BUTTONS
     ========================================== */
  
  .cta-button {
    display: inline-block;
    padding: 16px 32px;
    font-size: 18px;
    font-weight: 600;
    border-radius: 8px;
    text-decoration: none;
    transition: all 0.2s ease;
    cursor: pointer;
  }

  .cta-primary {
    background: var(--color-primary, #3b82f6);
    color: #ffffff;
    border: 2px solid var(--color-primary, #3b82f6);
  }

  .cta-primary:hover {
    background: var(--color-primary-hover, #2563eb);
    transform: translateY(-2px);
    box-shadow: 0 8px 16px rgba(59, 130, 246, 0.3);
  }

  .cta-secondary {
    background: transparent;
    color: var(--color-primary, #60a5fa);
    border: 2px solid var(--color-primary, #3b82f6);
  }

  .cta-secondary:hover {
    background: var(--color-primary-soft, rgba(59, 130, 246, 0.1));
    transform: translateY(-2px);
  }

  .cta-primary-large {
    padding: 20px 48px;
    font-size: 20px;
  }

  /* ==========================================
     ANIMATIONS
     ========================================== */
  
  @keyframes fadeUp {
    from {
      opacity: 0;
      transform: translateY(20px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  /* ==========================================
     RESPONSIVE BREAKPOINTS
     ========================================== */
  
  @media (max-width: 768px) {
    .two-column-layout,
    .dual-card-grid,
    .services-grid {
      grid-template-columns: 1fr;
    }

    .trust-content {
      flex-direction: column;
      gap: 24px;
    }

    .cta-group {
      flex-direction: column;
    }

    .cta-button {
      width: 100%;
      text-align: center;
    }
  }

  /* ==========================================
     REDUCED MOTION
     ========================================== */
  
  @media (prefers-reduced-motion: reduce) {
    *,
    *::before,
    *::after {
      animation-duration: 0.01ms !important;
      animation-iteration-count: 1 !important;
      transition-duration: 0.01ms !important;
    }
  }
</style>